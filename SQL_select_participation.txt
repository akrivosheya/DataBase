SELECT TOURISTS.NAME, TOURISTS.LAST_NAME, TOURISTS.BIRTH, COMPETITIONS.NAME
   FROM (PARTICIPATION LEFT OUTER JOIN TOURISTS ON TOURISTS.ID = PARTICIPATION.PARTICIPANT)
      LEFT OUTER JOIN COMPETITIONS ON COMPETITIONS.ID = PARTICIPATION.COMPETITION