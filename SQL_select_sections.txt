SELECT SECTIONS.NAME AS NAME, DIRECTORS.NAME AS DIRECTOR_NAME, DIRECTORS.LAST_NAME AS DIRECTOR_LAST_NAME, DIRECTORS.BIRTH AS DIRECTOR_BIRTH
   FROM SECTIONS LEFT OUTER JOIN DIRECTORS ON SECTIONS.DIRECTOR = DIRECTORS.ID