SELECT TOURISTS.NAME AS COACH_NAME, TOURISTS.LAST_NAME AS COACH_LAST_NAME, TOURISTS.BIRTH AS COACH_BIRTH, TRAINS.GROUP_ID AS GROUP_ID, TRAININGS.NAME AS TRAINING
   FROM (TRAINS LEFT OUTER JOIN TOURISTS ON TOURISTS.ID = TRAINS.COACH)
      LEFT OUTER JOIN TRAININGS ON TRAININGS.ID = TRAINS.TRAINING